# Auto Work 项目功能与设计文档

## 1. 项目概述

`Auto Work` 是一款使用 Tauri (Rust + React) 构建的桌面端数据处理工具。其核心目标是让用户能够创建可复用的**模板**，通过这些模板，可以对结构化的数据文件（目前主要是 Excel）执行一系列的提取、转换和计算操作，最终生成结果。

## 2. 核心概念与实体

整个应用围绕着以下几个核心概念构建：

### 2.1. 模板 (Template)

- **定义**：模板是最高层级的实体，代表一个完整、可复用的数据处理流程。
- **功能**：用户可以在主界面左上角创建、编辑、删除模板。每个模板都是一个独立的工作区，包含了专属的文件占位符、参数定义和数据处理步骤（即数据单元）。

### 2.2. 文件占位符 (File Placeholder)

- **定义**：这**不是**实际的文件，而是一个“文件坑位”的定义。例如，可以定义一个名为 `年度人口数据.xlsx` 的占位符。
- **功能**：
    - 在主界面左侧的“文件列表”中进行定义（添加/删除）。
    - 在创建“文件”类型的数据单元时，用户会从这些预先定义好的占位符中进行选择。
    - **设计目的**：这种设计将模板的【数据源定义】与【实际计算时使用的文件】解耦。使得模板可以被反复使用，每次运行时，即使用于计算的实际文件名不同（如 `2022年人口.xlsx`, `2023年人口.xlsx`），只要它们对应的是同一个占位符，模板就能正确执行。

### 2.3. 参数定义 (Parameter Definition)

- **定义**：在计算过程中可能需要用到的一些动态变量，例如 `年份`、`地区`、`修正系数` 等。
- **功能**：
    - 在“参数定义”卡片中，用户可以预先定义好这些参数的**键 (Key)**。
    - 在后续的计算执行环节，用户需要为这些键提供具体的**值 (Value)**。

### 2.4. 数据单元 (Data Cell)

- **定义**：这是整个数据处理流程的核心，本质上是一个处理步骤的序列（或管道）。每一个数据单元可以看成一个数据的集合。
- **类型**：
    1.  **文件 (File)**：从一个文件中提取一整列数据。此类型的单元需要关联一个【文件占位符】，并指定工作表名（`sheet`）和索引。
        * 索引：指定行号，列号，初始索引和结束索引。如果行号，列号都指定，那么就精准定义到了一个单元格（即数据长度为1的数据单元）
    2.  **计算 (Script)**：根据已有的数据单元进行指定运算符计算，包括：
       "+", "-","*",  "/",  "(", ")", 和
        {key: "group", label: "分组"} ： 把多个数据单元合并成一个数据单元
        {key: "sum", label: "连加"}：把多个数据单元连加成数据长度为1的数据单元,
        {key: "multi", label: "连乘"}把多个数据单元连乘成数据长度为1的数据单元,。
    3.  **其他数据单元 (Data)**：根据已有的数据单元和指定的初始索引和结束索引，或者精准的索引，确定一个更小范围的数据单位。
    4.  **参数 (Param)**：用于在计算中引用一个在运行时传入的【参数】的值。
    5.  **具体值 (Value)**：代表一个硬编码的常量值。

## 3. 关键工作流程

### 3.1. 模板定义工作流 (界面左侧与中间下方)

1.  **创建模板**：用户在“模板列表”中创建一个新的模板，例如“城市GDP计算模板”。
2.  **定义文件占位符**：用户在“文件列表”中添加所需的占位符，例如 `gdp.xlsx` 和 `population.xlsx`。
3.  **定义参数**：用户在“参数定义”中添加所需的参数键，例如 `year`。
4.  **编排数据单元**：用户在“模板数据集”中，通过“添加数据”按钮，一步步搭建处理流水线：
    - 创建一个名为“总人口”的数据单元，类型为“文件”，关联到 `population.xlsx` 占位符，并指定列为 `C`。
    - 创建一个名为“总GDP”的数据单元，类型为“文件”，关联到 `gdp.xlsx` 占位符，并指定列为 `B`。
    - 创建一个名为“人均GDP”的数据单元，类型为“计算”，脚本为 `[总GDP] / [总人口]`。

### 3.2. 计算执行工作流 (界面右侧)

1.  **设置参数值**：用户在“结果计算”卡片中，为预设的参数 `year` 提供具体的值，例如 `2023`。
2.  **上传临时文件**：用户点击“选择文件”按钮，上传**本次计算实际使用**的文件。例如，选择一个本地名为 `gdp_2023.xlsx` 的文件和 `pop_2023.xlsx` 的文件。
3.  **执行计算**：用户点击“开始运行”。
4.  **数据流转**：
    - 前端将 `template_id`、设置的参数值（`{"year": "2023"}`）、以及刚才上传的两个文件的**文件名和Base64内容**一并打包，发送给后端的 `execute_template` 命令。
    - 后端接收到数据，开始执行“人均GDP”的计算。当它需要 `[总人口]` 的值时，它会发现“总人口”数据单元依赖于 `population.xlsx` 这个占位符。
    - 后端会在本次上传的临时文件列表中查找名为 `population.xlsx` 的文件，找到后，读取其内容并从中提取 `C` 列的数据。
    - 计算完成后，后端将更新后的数据单元（包含计算结果）返回给前端。
5.  **展示结果**：前端刷新“模板数据集”表格，用户可以看到“人均GDP”等数据单元的计算结果。

## 4. 技术架构

-   **前端**: React + TypeScript + Ant Design
-   **后端**: Rust + Tauri + SQLx
-   **数据库**: SQLite
-   **通信**: 前后端通过 Tauri 的 `invoke` 机制进行异步消息通信。

## 5. 待办与未来工作

-   **后端计算逻辑**：目前“计算”类型的后端处理逻辑尚未完全实现。
-   **编辑功能**：“数据单元”和“文件占位符”等的编辑功能尚未实现。
-   **错误处理**：需要更精细的错误处理和用户反馈机制。 